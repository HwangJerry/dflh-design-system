# UI 동기화 체크리스트 - 화면별 구현 상태

**생성일**: 2026-08-14  
**대상**: dflh-saf-v2-swift v0.0.12 ↔ dflh-saf-v2-kotlin v0.1.0

---

## 📊 전체 요약

```
📱 총 화면 수: 9개

✅ 완전 동기화: 5개
   - 피드 목록/상세
   - 메시지 목록/상세  
   - 프로필
   - 동문 검색
   - 기부

⚠️ 부분 구현: 1개
   - 배지 표시 위치 차이

❌ 미구현: 3개
   - 로그인 (Kotlin)
   - 동문 상세 (Kotlin)
   - 메시지 작성 (Kotlin)

📈 동기화율: 56% (5/9)
```

---

## 🔐 1. 로그인 (Auth/Login)

### 1.1 로그인 화면

**상태**: 🔴 **CRITICAL** - Kotlin 완전 미구현

```
┌─────────────────────────────────────────┐
│ Swift        │ Kotlin                   │
│ ✅ LoginView │ ❌ LoginScreen 없음      │
│ 341줄        │ (SessionViewModel만 있음)│
└─────────────────────────────────────────┘
```

#### Swift 구현 현황

| 파일 | 내용 | 줄수 |
|------|------|------|
| `LoginView.swift` | 로그인 UI | 341줄 |
| `LoginModels.swift` | 모델 | ? |
| `SocialLoginCoordinator.swift` | 로직 | ? |
| `AppleLoginAdapter.swift` | Apple 인증 | ? |

#### Swift 컴포넌트 목록

```
LoginView
├─ ID/비밀번호 입력 필드 (DSTextField 사용)
├─ 로그인 버튼 (DSButton.Primary)
├─ 로딩 오버레이
├─ 에러 메시지 (DSColor.Error 텍스트)
├─ Kakao 로그인 버튼
├─ Apple 로그인 버튼
├─ 회원가입 링크 (Web)
└─ 아이디/비밀번호 찾기 링크 (Web)
```

#### Kotlin 필요 구현

```kotlin
// ❌ 완전히 없음 - 새로 작성 필요

// 필요한 파일
1. feature/auth/ui/LoginScreen.kt (300~400줄 예상)
2. feature/auth/ui/SocialLoginButton.kt
3. feature/auth/model/LoginFormState.kt
4. feature/auth/ui/components/LoginTextField.kt

// 필요한 컴포넌트
- ID/비밀번호 입력 폼
- 로그인 버튼
- 로딩 상태 + 오버레이
- 에러 메시지 표시
- Kakao 로그인 버튼
- 회원가입/아이디찾기 WebSheet
```

#### 체크리스트

```
실행 전
- [ ] Swift LoginView 코드 리뷰
- [ ] 사용할 DS 토큰 식별 (DSButton, DSTextField, DSTextStyle 등)
- [ ] 상태 정의 (로딩, 에러, 성공)

개발
- [ ] LoginScreen.kt 생성
- [ ] ID/비밀번호 입력 필드
- [ ] 로그인 API 호출
- [ ] 로딩 상태 UI
- [ ] 에러 메시지 표시
- [ ] Kakao 로그인 버튼
- [ ] 회원가입 링크 (WebSheet)

검증
- [ ] 입력 필드 높이 일치 (DSSpace.M로 설정)
- [ ] 버튼 스타일 일치 (DSButton.Primary)
- [ ] 텍스트 스타일 일치 (DSTextStyle.Body)
- [ ] 컬러 일치 (DSColor 사용)
- [ ] 에러 메시지 위치/스타일 일치
```

---

## 📰 2. 피드 (Feed)

### 2.1 피드 목록 화면

**상태**: ✅ **동기화 완료** (다만 Hero 섹션 검증 필요)

```
┌────────────────────────────────────────┐
│ Swift          │ Kotlin               │
│ ✅ FeedListView│ ✅ FeedScreen.kt     │
│ 20KB           │ 963줄                │
└────────────────────────────────────────┘
```

#### 구현 현황 비교

| 기능 | Swift | Kotlin | 상태 |
|------|-------|--------|------|
| Hero 섹션 | ✅ | ⚠️ | 부분 (검증 필요) |
| 피드 아이템 | ✅ | ✅ | ✅ 동기화 |
| 광고 항목 | ✅ | ✅ | ✅ 동기화 |
| 기부 배너 | ✅ | ✅ | ✅ 동기화 |
| 무한 스크롤 | ✅ | ✅ | ✅ 동기화 |
| 좋아요 버튼 | ✅ | ✅ | ✅ 동기화 |
| 댓글 카운트 | ✅ | ✅ | ✅ 동기화 |

#### 체크리스트

```
검증 항목
- [ ] Hero 섹션 높이 일치
- [ ] 배경색 일치 (DSColor.Background)
- [ ] 피드 아이템 높이 일치 (DSCard.Default)
- [ ] 아이템 간격 일치 (DSSpace.M)
- [ ] 광고 스타일 일치
- [ ] 배지 위치 일치
- [ ] 텍스트 스타일 일치 (DSTextStyle)
- [ ] 로딩 인디케이터 위치 일치
- [ ] 에러 상태 메시지 일치
```

---

### 2.2 피드 상세 화면

**상태**: ✅ **동기화 완료**

```
┌────────────────────────────────────────┐
│ Swift              │ Kotlin           │
│ ✅ FeedPostDetailView│ ✅ FeedScreen.kt│
│ 17KB               │ (동일 파일)     │
└────────────────────────────────────────┘
```

#### 구현 현황 비교

| 기능 | Swift | Kotlin | 상태 |
|------|-------|--------|------|
| 포스트 정보 | ✅ | ✅ | ✅ 동기화 |
| 댓글 목록 | ✅ | ✅ | ✅ 동기화 |
| 댓글 작성 | ✅ | ✅ | ✅ 동기화 |
| 댓글 삭제 | ✅ | ✅ | ✅ 동기화 |
| 좋아요 토글 | ✅ | ✅ | ✅ 동기화 |

#### 체크리스트

```
검증 항목
- [ ] 댓글 입력 필드 높이 일치
- [ ] 댓글 삭제 버튼 위치 일치
- [ ] 타임스탬프 포맷 일치 (DSDateFormatter)
- [ ] 좋아요 버튼 상태 표시 일치
- [ ] 댓글 로딩 표시 일치
```

---

## 💬 3. 메시지 (Messages)

### 3.1 메시지 목록 화면

**상태**: ✅ **동기화 완료** (배지만 미세한 차이)

```
┌────────────────────────────────────────┐
│ Swift           │ Kotlin              │
│ ✅ MessageListView│ ✅ MessagesScreen.kt│
│ ?KB             │ ?줄                 │
└────────────────────────────────────────┘
```

#### 구현 현황 비교

| 기능 | Swift | Kotlin | 상태 |
|------|-------|--------|------|
| 대화 목록 | ✅ | ✅ | ✅ 동기화 |
| 읽음 표시 | ✅ | ✅ | ✅ 동기화 |
| 배지 | ✅ | ⚠️ | 위치 다름 |
| 마지막 메시지 | ✅ | ✅ | ✅ 동기화 |
| 타임스탬프 | ✅ | ✅ | ✅ 동기화 |

#### 체크리스트

```
검증 항목
- [ ] 배지 위치 확인 (오른쪽? 위?)
- [ ] 배지 스타일 일치 (DSColor.Badge)
- [ ] 아이템 높이 일치 (DSSpace로 계산)
- [ ] 텍스트 스타일 일치 (DSTextStyle)
- [ ] 스크롤 가능 영역 테스트
```

**주의**: 배지 위치가 미세하게 다를 수 있음 - 실제 기기에서 확인 필수

---

### 3.2 메시지 상세 (대화) 화면

**상태**: ✅ **동기화 완료**

```
┌────────────────────────────────────────┐
│ Swift                    │ Kotlin       │
│ ✅ MessageConversationDetail│ ✅ MessagesScreen.kt│
│ ?KB                      │ ?줄          │
└────────────────────────────────────────┘
```

#### 구현 현황 비교

| 기능 | Swift | Kotlin | 상태 |
|------|-------|--------|------|
| 메시지 목록 | ✅ | ✅ | ✅ 동기화 |
| 입력 필드 | ✅ | ✅ | ✅ 동기화 |
| 전송 버튼 | ✅ | ✅ | ✅ 동기화 |
| SSE 실시간 | ✅ | ✅ | ✅ 동기화 |

#### 체크리스트

```
검증 항목
- [ ] 메시지 말풍선 스타일 일치 (내/상대 다르게)
- [ ] 입력 필드 높이 일치
- [ ] 버튼 위치/스타일 일치
- [ ] 타임스탬프 포맷 일치
- [ ] 키보드 표시/숨김 애니메이션
- [ ] SSE 연결 상태 표시 일치
```

---

### 3.3 메시지 작성 시트

**상태**: 🟠 **부분 미구현**

```
┌────────────────────────────────────────┐
│ Swift                   │ Kotlin       │
│ ✅ MessageComposeSheetView│ ❌ 없음      │
│ (Bottom Sheet)          │              │
└────────────────────────────────────────┘
```

#### Kotlin 필요 구현

```kotlin
// ❌ 새로 작성 필요
feature/messages/ui/MessageComposeSheet.kt

// 기능
- 동문 검색/선택
- 메시지 작성 필드
- 첨부파일 (나중에)
- 보내기 버튼
```

---

## 👥 4. 프로필 (Profile)

### 4.1 프로필 화면

**상태**: ✅ **동기화 완료**

```
┌────────────────────────────────────────┐
│ Swift         │ Kotlin               │
│ ✅ ProfileView│ ✅ ProfileScreen.kt  │
│ ?KB           │ ?줄                  │
└────────────────────────────────────────┘
```

#### 구현 현황 비교

| 기능 | Swift | Kotlin | 상태 |
|------|-------|--------|------|
| 프로필 사진 | ✅ | ✅ | ✅ 동기화 |
| 사용자 정보 | ✅ | ✅ | ✅ 동기화 |
| 직업 정보 | ✅ | ✅ | ✅ 동기화 |
| 편집 버튼 | ✅ | ✅ | ✅ 동기화 |
| 로그아웃 버튼 | ✅ | ✅ | ✅ 동기화 |

#### 체크리스트

```
검증 항목
- [ ] 프로필 사진 크기 일치 (DSSpace 계산)
- [ ] 텍스트 정렬 일치
- [ ] 버튼 스타일 일치 (DSButton)
- [ ] 여백 일치 (DSSpace)
- [ ] 로그아웃 버튼 위치 일치
```

---

### 4.2 프로필 편집

**상태**: ⚠️ **부분 구현**

#### Swift 구현

```
내장형 (ProfileView 내부):
├─ 이름 편집 필드
├─ 직업 선택 피커
├─ 사진 업로드
└─ 저장/취소 버튼
```

#### Kotlin 구현

```
ProfileFormState.kt 있음:
├─ ✅ 이름 편집
├─ ✅ 직업 선택
├─ ✅ 사진 업로드 API
├─ ⚠️ 진행률 표시 (구현 필요)
└─ ✅ 저장 로직
```

#### 부족 항목

```
❌ 1. 사진 업로드 진행률 UI
      - Swift: 진행률 표시 있음
      - Kotlin: 로직만 있음, UI 없음
      
   필요 구현:
   val uploadProgress: StateFlow<Float>
   
   UI:
   if (isUploading) {
       CircularProgressIndicator(
           progress = uploadProgress.value,
           modifier = Modifier.size(DSSpace.XL)
       )
   }
```

#### 체크리스트

```
검증 항목
- [ ] 입력 필드 높이 일치
- [ ] 유효성 검사 메시지 위치 일치
- [ ] 업로드 진행률 표시 추가 (필수)
- [ ] 로딩 상태 오버레이 일치
- [ ] 성공/에러 메시지 일치
```

---

## 🎓 5. 동문 (Alumni)

### 5.1 동문 검색 화면

**상태**: ✅ **동기화 완료**

```
┌────────────────────────────────────────┐
│ Swift          │ Kotlin              │
│ ✅ AlumniSearchView│ ✅ AlumniScreen.kt│
│ 17KB           │ 963줄               │
└────────────────────────────────────────┘
```

#### 구현 현황 비교

| 기능 | Swift | Kotlin | 상태 |
|------|-------|--------|------|
| 검색 필드 | ✅ | ✅ | ✅ 동기화 |
| 필터 | ✅ | ✅ | ✅ 동기화 |
| 검색 결과 | ✅ | ✅ | ✅ 동기화 |
| 필터 초기화 | ✅ | ✅ | ✅ 동기화 |
| 결과 로딩 | ✅ | ✅ | ✅ 동기화 |

#### 체크리스트

```
검증 항목
- [ ] 검색 필드 높이 일치
- [ ] 필터 칩 스타일 일치 (DSChip or DSButton)
- [ ] 결과 리스트 아이템 높이 일치
- [ ] 필터 적용 애니메이션 일치
- [ ] 로딩 표시 위치 일치
```

---

### 5.2 동문 상세 화면

**상태**: 🔴 **Kotlin 완전 미구현**

```
┌────────────────────────────────────────┐
│ Swift              │ Kotlin            │
│ ✅ AlumniDetailView│ ❌ AlumniDetailScreen│
│ 12KB               │ 없음              │
└────────────────────────────────────────┘
```

#### Swift 컴포넌트

```
AlumniDetailView
├─ 프로필 정보 (이름, 사진, 직업 등)
├─ 연락처 (마스킹됨)
├─ 메시지 보내기 버튼
└─ 닫기 버튼
```

#### Kotlin 필요 구현

```kotlin
// ❌ 새로 작성 필요
feature/alumni/ui/AlumniDetailScreen.kt (300~400줄 예상)

// 기능
- 상세 정보 표시
- 메시지 버튼 (Messages로 이동)
- 다이얼로그 또는 화면 형태
```

#### 체크리스트

```
실행 전
- [ ] Swift AlumniDetailView 분석
- [ ] 사용할 컴포넌트 식별 (DSCard, DSButton)
- [ ] 상태 정의 (로딩, 에러)

개발
- [ ] AlumniDetailScreen.kt 생성
- [ ] 정보 표시 레이아웃
- [ ] 메시지 버튼 구현
- [ ] 닫기/뒤로가기 버튼

검증
- [ ] 레이아웃 일치
- [ ] 텍스트 스타일 일치
- [ ] 버튼 스타일 일치 (DSButton)
- [ ] 색상 일치 (DSColor)
- [ ] 간격 일치 (DSSpace)
```

---

## 💰 6. 기부 (Donation)

### 6.1 기부 탭

**상태**: ✅ **동기화 완료**

```
┌────────────────────────────────────────┐
│ Swift           │ Kotlin              │
│ ✅ DonationLinkView│ ✅ DonationWebSheet│
│ ?KB             │ ?줄                 │
└────────────────────────────────────────┘
```

#### 구현 현황 비교

| 기능 | Swift | Kotlin | 상태 |
|------|-------|--------|------|
| 기부 버튼 | ✅ | ✅ | ✅ 동기화 |
| WebView | ✅ | ✅ | ✅ 동기화 |
| URL 검증 | ✅ | ✅ | ✅ 동기화 |
| 로딩 표시 | ✅ | ✅ | ✅ 동기화 |

#### 체크리스트

```
검증 항목
- [ ] 버튼 텍스트 일치 ("기부하기")
- [ ] 버튼 색상 일치 (DSColor.Primary)
- [ ] WebView 높이 일치 (전체 화면)
- [ ] 로딩 인디케이터 위치 일치
- [ ] 닫기 버튼 위치 일치
```

---

## 🔔 7. 푸시 알림 (Push)

### 7.1 알림 권한 프롬프트

**상태**: ✅ **동기화 완료**

```
┌────────────────────────────────────────────┐
│ Swift                                      │
│ ✅ APNs 자동 요청                          │
│ (AppDelegate에서 처리)                    │
├────────────────────────────────────────────┤
│ Kotlin                                     │
│ ✅ NotificationPermissionPrompt.kt        │
│ (Android 13+ 권한 요청)                   │
└────────────────────────────────────────────┘
```

#### 체크리스트

```
검증 항목
- [ ] 프롬프트 텍스트 일치
- [ ] 버튼 스타일 일치 (DSButton)
- [ ] 권한 거부 후 재요청 가능
- [ ] 설정 앱 이동 버튼 (Android)
```

---

## 🎨 디자인 토큰 일관성 검증

### 필수 검증 항목

#### 색상 (DSColor) ✅/❌

```
필수 사용:
- [ ] 배경색: DSColor.Background
- [ ] 텍스트 주색: DSColor.Text.primary
- [ ] 텍스트 부색: DSColor.Text.secondary
- [ ] 버튼 주색: DSColor.Primary
- [ ] 버튼 부색: DSColor.Secondary
- [ ] 경계선: DSColor.Border
- [ ] 에러: DSColor.Error
- [ ] 성공: DSColor.Success
- [ ] 경고: DSColor.Warning
```

#### 타이포그래피 (DSTextStyle) ✅/❌

```
필수 사용:
- [ ] 제목1: DSTextStyle.Headline1
- [ ] 제목2: DSTextStyle.Headline2
- [ ] 제목3: DSTextStyle.Headline3
- [ ] 본문: DSTextStyle.Body
- [ ] 본문 작음: DSTextStyle.BodySmall
- [ ] 캡션: DSTextStyle.Caption
- [ ] 강조: DSTextStyle.Emphasis
```

#### 간격 (DSSpace) ✅/❌

```
필수 사용:
- [ ] XS (4px): DSSpace.XS
- [ ] S (8px): DSSpace.S
- [ ] M (12px): DSSpace.M
- [ ] L (16px): DSSpace.L
- [ ] XL (20px): DSSpace.XL
- [ ] 2XL (24px): DSSpace.XXL
- [ ] 3XL (32px): DSSpace.XXXL
```

#### 컴포넌트 (DSCard, DSButton) ✅/❌

```
필수 검증:
- [ ] DSCard.Default 스타일
- [ ] DSCard.Elevated 스타일
- [ ] DSCard.Outlined 스타일
- [ ] DSButton.Primary 스타일
- [ ] DSButton.Secondary 스타일
- [ ] DSButton.Text 스타일
- [ ] 모든 상태 (normal, pressed, disabled)
```

---

## 📋 최종 검증 체크리스트

### 각 화면별 최종 체크 (코드 리뷰 전)

```
전체 화면:
- [ ] 모든 색상이 DSColor.* 사용
- [ ] 모든 텍스트가 DSTextStyle.* 사용
- [ ] 모든 여백이 DSSpace.* 사용
- [ ] 하드코딩된 값 없음 (Color, fontSize, padding 등)

각 화면:
- [ ] Swift 원본 분석 완료
- [ ] Kotlin 대응 파일 확인
- [ ] 레이아웃 일치 (스크린샷 비교)
- [ ] DS 토큰 일치
- [ ] 상태/에러 처리 동일
```

---

## 📌 우선순위별 작업 순서

### Phase 1 (긴급) - 이번 주
1. **로그인 (LoginScreen.kt)** - 사용자가 앱을 사용해야 함
2. **동문 상세 (AlumniDetailScreen.kt)** - 기능 완성
3. **프로필 편집 - 업로드 진행률** - UI 개선

### Phase 2 (높음) - 다음 주
1. **메시지 작성 시트** - 기능 완성
2. **전체 DS 토큰 검증** - 자동화 검사
3. **스크린샷 비교** - 시각적 일치 확인

### Phase 3 (중간) - 2주 후
1. **전체 UI 테스트** - 실제 기기
2. **성능 최적화** - 스크롤, 애니메이션
3. **접근성 검증** - 색약자, 스크린 리더

---

**생성**: 2026-08-14 by Claude Code  
**상태**: 진행 중 (매주 업데이트)  
**다음 업데이트**: 에이전트 자동 분석 결과 반영
